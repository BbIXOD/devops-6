apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: map
  name: map
spec:
  type: NodePort
  ports:
    - name: "3000"
      port: 3000
      targetPort: 80
      nodePort: 30000
  selector:
    io.kompose.service: map
